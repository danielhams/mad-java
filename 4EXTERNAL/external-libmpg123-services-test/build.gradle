apply plugin: 'java'

sourceSets {
    test {
        java {
            srcDirs = ['src/test']
        }
        resources {
            srcDirs = ['src']
        }
    }
}

repositories { mavenCentral() }

dependencies {
    compile project(':external-libmpg123-wrapper')
    compile project(':external-libmpg123-services')
}

tasks.withType(Test) {
  systemProperty "java.library.path", "../external-libmpg123-wrapper/lib"
}